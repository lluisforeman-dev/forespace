services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: forespace
      POSTGRES_USER: forespace
      POSTGRES_PASSWORD: forespace
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  postgres_data:
